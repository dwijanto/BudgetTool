Public Class MeHost

    Public Sub New()

        ' This call is required by the designer.
        InitializeComponent()

        ' Add any initialization after the InitializeComponent() call.
        Me.PictureBox1.Image = New Bitmap("images/stock_autofilter.png")
    End Sub
End Class
